"$schema": http://json.schemastore.org/prettierrc

trailingComma: "all"

overrides:
  - files: "*.md"
    options:
      embeddedLanguageFormatting: "off"
  - files: ".nvmrc"
    options:
      parser: "yaml"
