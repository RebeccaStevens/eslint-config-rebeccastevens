name: Type Check

on:
  pull_request:
  workflow_dispatch:
  workflow_call:
  push:
    branches:
      - "renovate/*"

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/prepare
      - run: pnpm run typecheck
